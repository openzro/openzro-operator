---
# Source: openzro-operator/crds/openzro.io_nbgroups.yaml
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: ozgroups.openzro.io
spec:
  group: openzro.io
  names:
    kind: OZGroup
    listKind: OZGroupList
    plural: ozgroups
    singular: ozgroup
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: OZGroup is the Schema for the ozgroups API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OZGroupSpec defines the desired state of OZGroup.
            properties:
              name:
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: Value is immutable
                  rule: self == oldSelf
            required:
            - name
            type: object
          status:
            description: OZGroupStatus defines the observed state of OZGroup.
            properties:
              conditions:
                items:
                  description: OZCondition defines a condition in OZSetupKey status.
                  properties:
                    lastProbeTime:
                      description: Last time we probed the condition.
                      format: date-time
                      type: string
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      format: date-time
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: |-
                        Status is the status of the condition.
                        Can be True, False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              groupID:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

---
# Source: openzro-operator/crds/openzro.io_nbpolicies.yaml
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: ozpolicies.openzro.io
spec:
  group: openzro.io
  names:
    kind: OZPolicy
    listKind: OZPolicyList
    plural: ozpolicies
    singular: ozpolicy
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: OZPolicy is the Schema for the ozpolicies API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OZPolicySpec defines the desired state of OZPolicy.
            properties:
              bidirectional:
                default: true
                type: boolean
              description:
                type: string
              destinationGroups:
                items:
                  minLength: 1
                  type: string
                type: array
              name:
                description: Name Policy name
                minLength: 1
                type: string
              ports:
                items:
                  format: int32
                  maximum: 65535
                  minimum: 0
                  type: integer
                type: array
              protocols:
                items:
                  enum:
                  - tcp
                  - udp
                  type: string
                type: array
              sourceGroups:
                items:
                  minLength: 1
                  type: string
                type: array
            required:
            - name
            type: object
          status:
            description: OZPolicyStatus defines the observed state of OZPolicy.
            properties:
              conditions:
                items:
                  description: OZCondition defines a condition in OZSetupKey status.
                  properties:
                    lastProbeTime:
                      description: Last time we probed the condition.
                      format: date-time
                      type: string
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      format: date-time
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: |-
                        Status is the status of the condition.
                        Can be True, False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              lastUpdatedAt:
                format: date-time
                type: string
              managedServiceList:
                items:
                  type: string
                type: array
              tcpPolicyID:
                type: string
              udpPolicyID:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

---
# Source: openzro-operator/crds/openzro.io_nbresources.yaml
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: ozresources.openzro.io
spec:
  group: openzro.io
  names:
    kind: OZResource
    listKind: OZResourceList
    plural: ozresources
    singular: ozresource
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: OZResource is the Schema for the ozresources API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OZResourceSpec defines the desired state of OZResource.
            properties:
              address:
                minLength: 1
                type: string
              groups:
                items:
                  minLength: 1
                  type: string
                type: array
              name:
                minLength: 1
                type: string
              networkID:
                type: string
                x-kubernetes-validations:
                - message: Value is immutable
                  rule: self == oldSelf
              policyName:
                type: string
              tcpPorts:
                items:
                  format: int32
                  type: integer
                type: array
              udpPorts:
                items:
                  format: int32
                  type: integer
                type: array
            required:
            - address
            - groups
            - name
            - networkID
            type: object
          status:
            description: OZResourceStatus defines the observed state of OZResource.
            properties:
              conditions:
                items:
                  description: OZCondition defines a condition in OZSetupKey status.
                  properties:
                    lastProbeTime:
                      description: Last time we probed the condition.
                      format: date-time
                      type: string
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      format: date-time
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: |-
                        Status is the status of the condition.
                        Can be True, False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              groups:
                items:
                  type: string
                type: array
              networkResourceID:
                type: string
              policyName:
                type: string
              tcpPorts:
                items:
                  format: int32
                  type: integer
                type: array
              udpPorts:
                items:
                  format: int32
                  type: integer
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

---
# Source: openzro-operator/crds/openzro.io_nbroutingpeers.yaml
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: ozroutingpeers.openzro.io
spec:
  group: openzro.io
  names:
    kind: OZRoutingPeer
    listKind: OZRoutingPeerList
    plural: ozroutingpeers
    singular: ozroutingpeer
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: OZRoutingPeer is the Schema for the ozroutingpeers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OZRoutingPeerSpec defines the desired state of OZRoutingPeer.
            properties:
              annotations:
                additionalProperties:
                  type: string
                type: object
              labels:
                additionalProperties:
                  type: string
                type: object
              nodeSelector:
                additionalProperties:
                  type: string
                type: object
              replicas:
                format: int32
                type: integer
              resources:
                description: ResourceRequirements describes the compute resource requirements.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This is an alpha field and requires enabling the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              tolerations:
                items:
                  description: |-
                    The pod this Toleration is attached to tolerates any taint that matches
                    the triple <key,value,effect> using the matching operator <operator>.
                  properties:
                    effect:
                      description: |-
                        Effect indicates the taint effect to match. Empty means match all taint effects.
                        When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                      type: string
                    key:
                      description: |-
                        Key is the taint key that the toleration applies to. Empty means match all taint keys.
                        If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                      type: string
                    operator:
                      description: |-
                        Operator represents a key's relationship to the value.
                        Valid operators are Exists and Equal. Defaults to Equal.
                        Exists is equivalent to wildcard for value, so that a pod can
                        tolerate all taints of a particular category.
                      type: string
                    tolerationSeconds:
                      description: |-
                        TolerationSeconds represents the period of time the toleration (which must be
                        of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                        it is not set, which means tolerate the taint forever (do not evict). Zero and
                        negative values will be treated as 0 (evict immediately) by the system.
                      format: int64
                      type: integer
                    value:
                      description: |-
                        Value is the taint value the toleration matches to.
                        If the operator is Exists, the value should be empty, otherwise just a regular string.
                      type: string
                  type: object
                type: array
            type: object
          status:
            description: OZRoutingPeerStatus defines the observed state of OZRoutingPeer.
            properties:
              conditions:
                items:
                  description: OZCondition defines a condition in OZSetupKey status.
                  properties:
                    lastProbeTime:
                      description: Last time we probed the condition.
                      format: date-time
                      type: string
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      format: date-time
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: |-
                        Status is the status of the condition.
                        Can be True, False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              networkID:
                type: string
              routerID:
                type: string
              setupKeyID:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

---
# Source: openzro-operator/crds/openzro.io_nbsetupkeys.yaml
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: ozsetupkeys.openzro.io
spec:
  group: openzro.io
  names:
    kind: OZSetupKey
    listKind: OZSetupKeyList
    plural: ozsetupkeys
    singular: ozsetupkey
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: OZSetupKey is the Schema for the ozsetupkeys API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OZSetupKeySpec defines the desired state of OZSetupKey.
            properties:
              managementURL:
                description: ManagementURL optional, override operator management
                  URL
                type: string
              secretKeyRef:
                description: SecretKeyRef is a reference to the secret containing
                  the setup key
                properties:
                  key:
                    description: The key of the secret to select from.  Must be a
                      valid secret key.
                    type: string
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  optional:
                    description: Specify whether the Secret or its key must be defined
                    type: boolean
                required:
                - key
                type: object
                x-kubernetes-map-type: atomic
            required:
            - secretKeyRef
            type: object
          status:
            description: OZSetupKeyStatus defines the observed state of OZSetupKey.
            properties:
              conditions:
                items:
                  description: OZCondition defines a condition in OZSetupKey status.
                  properties:
                    lastProbeTime:
                      description: Last time we probed the condition.
                      format: date-time
                      type: string
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      format: date-time
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: |-
                        Status is the status of the condition.
                        Can be True, False, Unknown.
                      type: string
                    type:
                      description: Type is the type of the condition.
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

---
# Source: openzro-operator/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: openzro-operator
  labels:
    helm.sh/chart: openzro-operator-0.1.1
    app.kubernetes.io/name: openzro-operator
    app.kubernetes.io/instance: openzro-operator
    app.kubernetes.io/version: "v0.1.0"
    app.kubernetes.io/managed-by: Helm
automountServiceAccountToken: true
---
# Source: openzro-operator/templates/rbac.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: openzro-operator
  labels:
    helm.sh/chart: openzro-operator-0.1.1
    app.kubernetes.io/name: openzro-operator
    app.kubernetes.io/instance: openzro-operator
    app.kubernetes.io/version: "v0.1.0"
    app.kubernetes.io/managed-by: Helm
rules:
- apiGroups:
  - openzro.io
  resources:
  - ozsetupkeys
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - openzro.io
  resources:
  - ozsetupkeys/finalizers
  verbs:
  - update
- apiGroups:
  - openzro.io
  resources:
  - ozsetupkeys/status
  verbs:
  - get
  - patch
  - update
- apiGroups:
  - ""
  resources:
  - pods
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - get
  - list
  - watch
---
# Source: openzro-operator/templates/rbac.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: openzro-operator
  labels:
    helm.sh/chart: openzro-operator-0.1.1
    app.kubernetes.io/name: openzro-operator
    app.kubernetes.io/instance: openzro-operator
    app.kubernetes.io/version: "v0.1.0"
    app.kubernetes.io/managed-by: Helm
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: openzro-operator
subjects:
- kind: ServiceAccount
  name: openzro-operator
  namespace: production
---
# Source: openzro-operator/templates/rbac.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: openzro-operator
  labels:
    helm.sh/chart: openzro-operator-0.1.1
    app.kubernetes.io/name: openzro-operator
    app.kubernetes.io/instance: openzro-operator
    app.kubernetes.io/version: "v0.1.0"
    app.kubernetes.io/managed-by: Helm
rules:
- apiGroups:
  - ""
  resources:
  - configmaps
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - coordination.k8s.io
  resources:
  - leases
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - ""
  resources:
  - events
  verbs:
  - create
  - patch
---
# Source: openzro-operator/templates/rbac.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: openzro-operator
  labels:
    helm.sh/chart: openzro-operator-0.1.1
    app.kubernetes.io/name: openzro-operator
    app.kubernetes.io/instance: openzro-operator
    app.kubernetes.io/version: "v0.1.0"
    app.kubernetes.io/managed-by: Helm
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: openzro-operator
subjects:
- kind: ServiceAccount
  name: openzro-operator
  namespace: production
---
# Source: openzro-operator/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: openzro-operator-metrics
  labels:
    helm.sh/chart: openzro-operator-0.1.1
    app.kubernetes.io/name: openzro-operator
    app.kubernetes.io/instance: openzro-operator
    app.kubernetes.io/version: "v0.1.0"
    app.kubernetes.io/managed-by: Helm
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app.kubernetes.io/name: openzro-operator
    app.kubernetes.io/instance: openzro-operator
---
# Source: openzro-operator/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: openzro-operator-webhook-service
  labels:
    helm.sh/chart: openzro-operator-0.1.1
    app.kubernetes.io/name: openzro-operator
    app.kubernetes.io/instance: openzro-operator
    app.kubernetes.io/version: "v0.1.0"
    app.kubernetes.io/managed-by: Helm
spec:
  type: ClusterIP
  ports:
  - name: https
    port: 443
    protocol: TCP
    targetPort: 9443
  selector:
    app.kubernetes.io/name: openzro-operator
    app.kubernetes.io/instance: openzro-operator
---
# Source: openzro-operator/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: openzro-operator
  labels:
    app.kubernetes.io/component: operator
    helm.sh/chart: openzro-operator-0.1.1
    app.kubernetes.io/name: openzro-operator
    app.kubernetes.io/instance: openzro-operator
    app.kubernetes.io/version: "v0.1.0"
    app.kubernetes.io/managed-by: Helm
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: openzro-operator
      app.kubernetes.io/instance: openzro-operator
  template:
    metadata:
      labels:
        app.kubernetes.io/component: operator
        helm.sh/chart: openzro-operator-0.1.1
        app.kubernetes.io/name: openzro-operator
        app.kubernetes.io/instance: openzro-operator
        app.kubernetes.io/version: "v0.1.0"
        app.kubernetes.io/managed-by: Helm
    spec:
      serviceAccountName: openzro-operator
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: openzro-operator
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
          image: "docker.io/openzro/openzro-operator:v0.1.0"
          imagePullPolicy: IfNotPresent
          command:
          - /manager
          args:
          - --metrics-bind-address=:8080
          - --leader-elect
          - --health-probe-bind-address=:8081
          - --webhook-cert-path=/tmp/k8s-webhook-server/serving-certs
          ports:
            - name: webhook-server
              containerPort: 443
              protocol: TCP
          livenessProbe:
            failureThreshold: 3
            httpGet:
              path: /healthz
              port: 8081
              scheme: HTTP
            initialDelaySeconds: 15
            periodSeconds: 20
            successThreshold: 1
            timeoutSeconds: 1
          readinessProbe:
            failureThreshold: 3
            httpGet:
              path: /readyz
              port: 8081
              scheme: HTTP
            initialDelaySeconds: 5
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 1
          resources:
            {}
          volumeMounts:
          - mountPath: /tmp/k8s-webhook-server/serving-certs
            name: webhook-certs
            readOnly: true
      volumes:
      - name: webhook-certs
        secret:
          defaultMode: 420
          secretName: openzro-operator-tls
---
# Source: openzro-operator/templates/webhook.yaml
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: openzro-operator-serving-cert
  namespace: production
  labels:
    helm.sh/chart: openzro-operator-0.1.1
    app.kubernetes.io/name: openzro-operator
    app.kubernetes.io/instance: openzro-operator
    app.kubernetes.io/version: "v0.1.0"
    app.kubernetes.io/managed-by: Helm
spec:
  dnsNames:
  - openzro-operator-webhook-service.production.svc
  - openzro-operator-webhook-service.production.svc.cluster.local
  issuerRef:
    kind: Issuer
    name: openzro-operator-selfsigned-issuer
  secretName: openzro-operator-tls
---
# Source: openzro-operator/templates/webhook.yaml
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: openzro-operator-selfsigned-issuer
  namespace: production
  labels:
    helm.sh/chart: openzro-operator-0.1.1
    app.kubernetes.io/name: openzro-operator
    app.kubernetes.io/instance: openzro-operator
    app.kubernetes.io/version: "v0.1.0"
    app.kubernetes.io/managed-by: Helm
spec:
  selfSigned: {}
---
# Source: openzro-operator/templates/webhook.yaml
apiVersion: admissionregistration.k8s.io/v1
kind: MutatingWebhookConfiguration
metadata:
  annotations:
    cert-manager.io/inject-ca-from: production/openzro-operator-serving-cert
  name: openzro-operator-mpod-webhook
  labels:
    helm.sh/chart: openzro-operator-0.1.1
    app.kubernetes.io/name: openzro-operator
    app.kubernetes.io/instance: openzro-operator
    app.kubernetes.io/version: "v0.1.0"
    app.kubernetes.io/managed-by: Helm
webhooks:
- clientConfig:
    service:
      name: openzro-operator-webhook-service
      namespace: production
      path: /mutate--v1-pod
  failurePolicy: Fail
  name: mpod-v1.openzro.io
  admissionReviewVersions:
  - v1
  objectSelector:
    matchExpressions:
    - key: app.kubernetes.io/name
      operator: NotIn
      values:
      - openzro-operator
  rules:
  - apiGroups:
    - ""
    apiVersions:
    - v1
    operations:
    - CREATE
    resources:
    - pods
  sideEffects: None
---
# Source: openzro-operator/templates/webhook.yaml
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingWebhookConfiguration
metadata:
  annotations:
    cert-manager.io/inject-ca-from: production/openzro-operator-serving-cert
  name: openzro-operator-vnbsetupkey-webhook
  labels:
    helm.sh/chart: openzro-operator-0.1.1
    app.kubernetes.io/name: openzro-operator
    app.kubernetes.io/instance: openzro-operator
    app.kubernetes.io/version: "v0.1.0"
    app.kubernetes.io/managed-by: Helm
webhooks:
- clientConfig:
    service:
      name: openzro-operator-webhook-service
      namespace: production
      path: /validate-openzro-io-v1-ozsetupkey
  failurePolicy: Fail
  name: vnbsetupkey-v1.openzro.io
  admissionReviewVersions:
  - v1
  rules:
  - apiGroups:
    - openzro.io
    apiVersions:
    - v1
    operations:
    - CREATE
    - UPDATE
    resources:
    - "ozsetupkeys"
  sideEffects: None
---
# Source: openzro-operator/templates/pre-delete.yaml
apiVersion: batch/v1
kind: Job
metadata:
  name: openzro-operator-delete-routers
  labels:
    app.kubernetes.io/component: operator
    helm.sh/chart: openzro-operator-0.1.1
    app.kubernetes.io/name: openzro-operator
    app.kubernetes.io/instance: openzro-operator
    app.kubernetes.io/version: "v0.1.0"
    app.kubernetes.io/managed-by: Helm
  annotations:
    helm.sh/hook: pre-delete
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
spec:
  backOffLimit: 3
  template:
    metadata:
      name: openzro-operator
      labels:
        app.kubernetes.io/component: operator
        helm.sh/chart: openzro-operator-0.1.1
        app.kubernetes.io/name: openzro-operator
        app.kubernetes.io/instance: openzro-operator
        app.kubernetes.io/version: "v0.1.0"
        app.kubernetes.io/managed-by: Helm
    spec:
      containers:
      - name: pre-delete
        image: "bitnami/kubectl:latest"
        args:
        - delete
        - --all
        - -A
        - --cascade=foreground
        - --ignore-not-found
        - OZRoutingPeer
      serviceAccountName: openzro-operator
      restartPolicy: Never
---
# Source: openzro-operator/templates/pre-delete.yaml
apiVersion: batch/v1
kind: Job
metadata:
  name: openzro-operator-delete-policies
  labels:
    app.kubernetes.io/component: operator
    helm.sh/chart: openzro-operator-0.1.1
    app.kubernetes.io/name: openzro-operator
    app.kubernetes.io/instance: openzro-operator
    app.kubernetes.io/version: "v0.1.0"
    app.kubernetes.io/managed-by: Helm
  annotations:
    helm.sh/hook: pre-delete
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
spec:
  backOffLimit: 3
  template:
    metadata:
      name: openzro-operator
      labels:
        app.kubernetes.io/component: operator
        helm.sh/chart: openzro-operator-0.1.1
        app.kubernetes.io/name: openzro-operator
        app.kubernetes.io/instance: openzro-operator
        app.kubernetes.io/version: "v0.1.0"
        app.kubernetes.io/managed-by: Helm
    spec:
      containers:
      - name: pre-delete
        image: "bitnami/kubectl:latest"
        args:
        - delete
        - --all
        - --cascade=foreground
        - --ignore-not-found
        - OZPolicy
      serviceAccountName: openzro-operator
      restartPolicy: Never
